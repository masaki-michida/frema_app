.mypage
  -# wrapperクラスにflexをてきよう
  %h5= "#{@user.nickname}さんのマイページ" 
  %h6 出品した商品
  
  .mypage__user_info
    - @items.each do |item|
      = link_to item_path(item.id) do
        = item.name
  
  .mypage__menu
    = render 'layouts/sidemenu'
  